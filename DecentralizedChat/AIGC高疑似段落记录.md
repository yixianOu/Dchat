# AIGC高疑似段落记录

> 来源：/home/orician/Downloads/aigc_words.pdf
> 检测工具：PaperPass免费版
> 总体疑似度：40.47%（高+中+轻）

---

## 高度疑似（70%及以上）

### AI 94% - NATS Routes集群机制
```
NATS Routes是NATS实现多节点集群的核心机制。通过Routes，多个NATS服务器可以互联形成一个集
群，集群内的消息自动广播到所有节点。
Routes集群采用全mesh（全网状）拓扑结构：集群内的每个节点都与其他所有节点建立直接的TCP连接。
当消息发布到某个节点时，该节点会将消息转发给集群内的所有其他节点，再由各节点转发给本地订阅者。
```
**位置**：第2章 2.2.2 NATS Routes与集群架构

---

### AI 93% - 摘要第一段
```
互联网技术的持续演进使得即时通信深度融入了人们的日常社交与生产活动。然而，当前占据市场主导地
位的中心化即时通信工具（例如微信、QQ等）在数据隐私层面存在显著隐患——用户的聊天内容、社交关系
等敏感信息悉数交由平台托管，由此带来了隐私泄露、数据滥用以及服务中断等多重风险。传统的去中心化通
信路径如P2P模式虽能将数据保留在本地，却受制于NAT穿越障碍、离线消息难以投递等现实瓶颈。
```
**位置**：摘要

---

### AI 91% - 密钥派生原理
```
本项目采用Ed25519作为身份签名密钥（用于NATS JWT认证），但不直接使用其进行消息加密。原因如下：
首先，Ed25519是专为数字签名设计的曲线，不具备直接加密能力；其次，根据密码学最佳实践，签名密钥
与加密密钥应分离，避免单点失效导致既丢失身份认证又泄露通信内容；最后，通过Ed25519私钥派生X25519
密钥对（基于曲线同源特性），既保持身份一致性，又获得ECDH密钥交换能力，实现端到端加密。
```
**位置**：第4章 4.2.1 密钥体系

---

### AI 86% - Hub认证机制
```
Hub支持两种认证模式：默认无鉴权模式和JWT认证模式，以适应不同的部署场景。
**默认无鉴权模式**：Hub默认不启用认证，任何LeafNode都可以连接。这种模式便于快速测试和公网
共享部署，降低了使用门槛。在这种模式下，虽然Hub不对连接进行身份验证，但消息的端到端加密仍然保
证了通信安全。
```
**位置**：第4章 4.1.3 Hub认证机制

---

### AI 84% - 跨平台应用开发
```
（5）**跨平台应用开发**：基于Wails框架的跨平台桌面应用开发，构建用户友好的图形交互界面。
```
**位置**：第6章 6.1 工作总结

---

### AI 83% - Ed25519密钥紧凑特性
```
（4）**密钥紧凑**：Ed25519公钥仅32字节，签名仅64字节，传输开销小。
```
**位置**：第2章 2.3.2 Ed25519与X25519密钥体系

---

### AI 76% - P2P技术挑战
```
但P2P面临技术挑战：**NAT穿透困难**（大多数设备位于NAT后，无公网IP）；**离线消息难以实现**
```
**位置**：第2章 2.1.3 纯P2P架构

---

### AI 70% - 摘要第二段
```
本论文设计并实现了一套基于NATS的去中心化即时通信系统。系统采用C/S连接架构，但服务端仅转发
消息，不存储用户数据；所有数据保存在本地SQLite数据库；通过NATS Routes构建多节点Hub集群实现服
务端去中心化；采用Ed25519/X25519双密钥体系和NaCl Box端到端加密保障通信安全。
```
**位置**：摘要

---

### AI 70% - 研究背景与意义
```
微信、WhatsApp等主流应用采用中心化架构，存在**数据主权旁落**（平台掌控用户数据）、**隐私保护缺
位**（元数据被记录）、**单点故障风险**（服务器故障影响所有用户）等问题。
```
**位置**：第1章 1.1 研究背景与意义

---

### AI 70% - 中心化方案
```
微信、WhatsApp等采用中心化架构，数据存储在平台服务器。部分平台引入端到端加密（如WhatsApp使
用Signal协议），但根本局限在于**数据仍存储在服务器上**，平台可获知通信元数据。
```
**位置**：第1章 1.2.1 中心化方案

---

### AI 70% - 架构设计核心思想
```
这种架构设计的核心思想是：将"连接"和"数据"分离。连接采用C/S架构保证了技术简单性和可靠性；数据
采用完全本地存储保证了用户的数据主权。相比于P2P架构，本方案避免了NAT穿透的技术难题；相比于传
统C/S架构，本方案实现了数据主权归属于用户。
```
**位置**：第2章 2.1.4 本项目架构设计

---

### AI 70% - LeafNode层级扩展
```
LeafNode是NATS提供的层级扩展机制，允许将本地NATS服务器作为叶子节点连接到远程Hub集群。
这种设计非常适合边缘计算场景，可以在本地部署轻量级NATS服务器，再通过LeafNode连接到中心集群。
```
**位置**：第2章 2.2.3 LeafNode与层级扩展

---

### AI 70% - LeafNode特点
```
（3）**安全隔离**：LeafNode可以配置本地账户体系，限制本地客户端的权限，实现安全隔离。
（4）**自动重连**：LeafNode与Hub的连接断开后会自动重连，保证连接的可靠性。
```
**位置**：第2章 2.2.3 LeafNode与层级扩展

---

### AI 70% - 离线同步
```
（3）**离线同步**：用户上线后，通过Pull Consumer从Stream拉取离线期间的消息，处理完成后发送
ACK确认。
```
**位置**：第2章 2.2.1 JetStream消息系统

---

### AI 70% - 应用层描述
```
**应用层**：基于Wails框架构建的桌面应用，包含React前端和Go后端。前端负责用户界面展示，后端
负责业务逻辑处理，包括加密解密、数据库操作、NATS客户端管理等。
```
**位置**：第3章 3.3.1 总体架构

---

### AI 70% - 集群层描述
```
**集群层**：公网部署的NATS Hub集群，通过Routes形成全mesh网络。集群负责消息的跨节点转发，
实现多节点去中心化部署。
```
**位置**：第3章 3.3.1 总体架构

---

### AI 70% - 应用内置LeafNode
```
本系统的核心设计之一是应用内置NATS Server作为LeafNode。这种设计带来了多重优势：本地客户端与
LeafNode的通信无需网络传输和认证，简化了架构；LeafNode作为本地代理，统一处理与远程Hub的连接
管理；应用完全控制LeafNode的生命周期，可以根据需要动态配置。
```
**位置**：第4章 4.1.1 应用内置的NATS LeafNode

---

### AI 70% - NATS客户端连接
```
应用内的NATS客户端连接到本地LeafNode（127.0.0.1:4222），这种本地连接具有特殊性：由于完全在应
用内部，本地连接无需任何认证。这种设计简化了认证流程，避免了不必要的凭证管理。
```
**位置**：第4章 4.1.2 NATS客户端与本地LeafNode连接

---

### AI 70% - NATS客户端功能
```
NATS客户端的核心功能包括连接管理、主题订阅和消息发布。连接管理负责建立和维护与本地LeafNode
的TCP连接，支持自动重连。主题订阅负责订阅感兴趣的消息主题，注册消息处理回调。消息发布负责将加密
后的消息发布到指定主题。
```
**位置**：第4章 4.1.2 NATS客户端与本地LeafNode连接

---

### AI 70% - Hub集群Routes配置
```
Hub集群通过Routes形成全mesh网络，实现服务端的去中心化部署。
**Routes全mesh架构**：集群内每个Hub节点都与其他所有节点建立直接的TCP连接。当消息发布到某
个节点时，该节点将消息转发给集群内所有其他节点，再由各节点转发给本地连接的客户端。
```
**位置**：第4章 4.1.4 Hub集群Routes配置

---

### AI 70% - 去中心化体现
```
**去中心化体现**：尽管存在集群边界限制，Routes集群仍然实现了服务端的去中心化：集群内多个Hub
地位平等，无主节点，任一Hub故障不影响集群整体服务，集群可水平扩展（需预先配置），不同集群之间可
通过Gateway互联。
```
**位置**：第4章 4.1.4 Hub集群Routes配置

---

### AI 70% - X25519加密密钥
```
**X25519加密密钥**：X25519是基于Curve25519曲线的密钥交换算法，用于ECDH密钥协商。在本系
统中，X25519密钥对从Ed25519私钥派生而来，用于NaCl Box加密时的密钥交换。这种派生基于Curve25519
与Ed25519曲线的同源特性，是确定性的但不可逆的。
```
**位置**：第4章 4.2.1 密钥体系

---

### AI 70% - 本地存储
```
**本地存储**：用户的私钥以文件形式存储在~/.dchat/目录下：operator.nk是Operator私钥（创建时生
成，应离线保存），account.nk是Account私钥，user.seed是User种子（从中可派生所有密钥），user.creds
是JWT凭证文件。
```
**位置**：第4章 4.2.1 密钥体系

---

### AI 70% - 生成临时密钥对
```
（1）生成临时密钥对：发送方生成临时的X25519密钥对（ephemeralPriv, ephemeralPub）。
```
**位置**：第4章 4.2.2 私聊加密实现

---

### AI 70% - 数据库表结构
```
系统设计了conversations、messages、friend_pub_keys、group_sym_keys四个核心表。
```
**位置**：第4章 4.3.1 数据库设计

---

### AI 70% - 历史查询
```
**历史查询**：系统支持按会话分页查询历史消息。查询时根据会话ID和偏移量获取指定数量的消息，按
时间戳升序排列。这种设计支持无限滚动加载，用户滚动到顶部时自动加载更早的消息。
```
**位置**：第4章 4.3.2 消息搜索与历史查询

---

### AI 70% - 上线同步流程
```
**上线同步流程**：用户上线后，系统执行以下同步流程：首先创建Pull Consumer订阅用户的离线消息主
题；然后循环拉取消息，每次拉取一批（如100条）；对于每条消息，解密后保存到本地数据库，并通知前端
展示；处理完成后发送ACK确认；继续拉取直到没有新消息。
```
**位置**：第4章 4.3.3 离线消息同步

---

### AI 70% - 加载配置
```
（1）**加载配置**：读取~/.dchat/config.json中的配置，包括Hub地址、用户身份信息等。如果配置文件
不存在，使用默认配置。
```
**位置**：第4章 4.4.1 应用启动流程

---

### AI 70% - 初始化SQLite
```
（3）**初始化SQLite**：打开或创建~/.dchat/dchat.db数据库文件，执行数据库迁移（migrations）确保
表结构最新。如果数据库损坏，尝试从备份恢复。
```
**位置**：第4章 4.4.1 应用启动流程

---

### AI 70% - 创建聊天服务
```
（5）**创建聊天服务**：初始化聊天服务，设置消息处理回调，加载好友列表和群组信息。
```
**位置**：第4章 4.4.1 应用启动流程

---

### AI 70% - 网络通信实现
```
（2）**网络通信实现**：基于NATS消息系统完成了应用内置LeafNode、Hub集群Routes互联、离线消
息同步等功能的实现。应用内置LeafNode的设计简化了客户端架构，Hub集群则实现了服务端层面的去中心
化部署。
```
**位置**：第6章 6.1 工作总结

---

### AI 70% - 安全机制实现
```
（3）**安全机制实现**：设计了Ed25519/X25519双密钥体系，实现了签名密钥与加密密钥的分离。采用
NaCl Box端到端加密方案，确保仅有通信双方能够读取消息内容。
```
**位置**：第6章 6.1 工作总结

---

### AI 70% - 创新点1
```
（1）**服务端零业务逻辑架构**：与传统C/S架构不同，本系统的服务端完全不参与业务处理，仅作为消
息传输层。这种设计使服务端实现了完全无状态，不存储任何用户数据，从根源上消除了服务端数据泄露的风
险。
```
**位置**：第6章 6.2 创新点

---

### AI 70% - 存在的不足1
```
（1）**公网Hub部署成本**：虽然系统设计支持多Hub去中心化部署，但运行公网Hub需要服务器资源
和持续运维，对个人用户而言门槛偏高。
```
**位置**：第6章 6.3 存在的不足

---

### AI 70% - 存在的不足2
```
（2）**群聊成员管理功能简单**：当前群聊功能较为基础，成员邀请、退出、移除等功能尚需完善，缺乏
群组管理员权限控制机制。
```
**位置**：第6章 6.3 存在的不足

---

### AI 70% - 未来展望1
```
（1）**移动端App开发**：开发iOS和Android客户端，实现全平台覆盖，扩大潜在用户群体。
```
**位置**：第6章 6.4 未来展望

---

### AI 70% - 未来展望3
```
（3）**文件传输功能**：基于端到端加密实现安全的文件传输能力，支持图片、文档等多种文件类型。
```
**位置**：第6章 6.4 未来展望

---

## 中度疑似（60%-69%）

### AI 69% - 联邦式方案
```
联邦式架构（如XMPP、Matrix）由多个独立服务器组成网络，用户可选择信任的节点。相比中心化方案，
用户可自主选择服务商，网络具备抗审查性。但未解决根本问题——**用户数据依旧存储在服务器上**。
```
**位置**：第1章 1.2.2 联邦式方案

---

### AI 69% - LeafNode管理器功能
```
LeafNode管理器的核心功能包括Hub地址解析、Server配置构建和Server生命周期管理。Hub地址解析
支持配置多个Hub URL，格式为nats://host:port，解析后转换为LeafNode远程配置。
```
**位置**：第4章 4.1.1 应用内置的NATS LeafNode

---

### AI 69% - 群聊主题设计
```
**群聊主题**：群聊消息的主题格式为`dchat.grp.<gid>.msg`，其中gid是群组ID，由创建时随机生成。所有
群组成员订阅该主题，即可接收群消息。
```
**位置**：第4章 4.1.5 消息主题设计

---

### AI 69% - Conversation ID生成
```
Conversation ID的生成算法保证了确定性：将通信双方的用户ID按字典序排序，用":"连接后计算SHA-256
哈希，取前16字节编码为十六进制字符串。
```
**位置**：第4章 4.1.5 消息主题设计

---

### AI 69% - 密钥体系
```
本系统采用双密钥体系：Ed25519用于身份签名和认证，X25519用于加密密钥交换。这种分离设计的核
心原因是签名密钥和加密密钥的使用场景不同，分离可以提高安全性。
```
**位置**：第4章 4.2.1 密钥体系

---

### AI 69% - 群聊加密实现
```
### 4.2.3 群聊加密实现

群聊采用对称加密方案，所有群组成员共享同一个群密钥。
```
**位置**：第4章 4.2.3 群聊加密实现

---

### AI 69% - 群消息加密
```
**群消息加密**：发送群消息时，使用群密钥和随机Nonce通过AES-256-GCM加密消息内容。构造的
EncWire结构与私聊类似，但cipher字段使用群密钥加密。
```
**位置**：第4章 4.2.3 群聊加密实现

---

### AI 69% - 消息去重
```
**消息去重**：由于网络原因可能导致消息重复投递，系统通过消息ID进行去重。保存消息前检查数据库
是否已存在相同ID的消息，如果存在则跳过。
```
**位置**：第4章 4.3.3 离线消息同步

---

### AI 69% - 消息接收
```
**消息接收**：NATS客户端接收到消息后，调用handleIncoming()回调。后端解密消息，保存到数据库，
通过EventsEmit通知前端。前端监听message-received事件，更新消息列表。
```
**位置**：第4章 4.4.3 核心功能实现

---

### AI 69% - 创新点2
```
（2）**C/S架构下的数据主权**：在保留C/S架构简洁性与可靠性的前提下，通过本地数据存储实现了数
据主权回归用户。用户完全掌控自己的通信数据，无需信赖任何第三方。
```
**位置**：第6章 6.2 创新点

---

### AI 69% - 未来展望5
```
（5）**去中心化身份（DID）集成**：集成去中心化身份标识符（Decentralized Identifiers）技术，实现跨
平台、跨应用的身份认证。
```
**位置**：第6章 6.4 未来展望

---

### AI 68% - 摘要英文版
```
This thesis designs and implements a decentralized instant messaging system based on NATS. The system
adopts C/S connection architecture, but the server only relays messages without storing user data; all data is
saved in local SQLite database; multi-node Hub cluster is built via NATS Routes for server-side decentralization;
Ed25519/X25519 dual-key system and NaCl Box end-to-end encryption ensure communication security.
```
**位置**：Abstract

---

### AI 68% - 即时通信架构模式
```
即时通信系统架构可分为中心化C/S、联邦式和纯P2P三类，在数据存储位置、连接复杂度、去中心化程
度上各有特点。
```
**位置**：第2章 2.1 即时通信架构模式

---

### AI 68% - Wails与Electron对比
```
Electron是流行的跨平台框架，但存在**体积庞大**（需打包Chromium和Node.js，超100MB）、**资源占
用高**（独立Chromium进程）、**启动慢**等问题。
```
**位置**：第2章 2.4.2 Wails与Electron对比

---

### AI 68% - 用户身份管理
```
用户身份管理是系统的基础功能，包括用户注册、身份创建、密钥管理等。具体需求如下：
（1）**身份创建**：用户首次使用系统时，需要创建数字身份。系统自动生成Ed25519签名密钥对和派生
的X25519加密密钥对，作为用户的身份标识。
```
**位置**：第3章 3.1.1 用户身份管理

---

### AI 68% - 网络通信模块
```
## 4.1 网络通信模块

网络通信模块是系统的基础设施，负责建立和维护网络连接，实现消息的可靠传输。本节将详细介绍应用
内置NATS LeafNode的设计、NATS客户端连接、Hub认证机制、Routes集群配置以及消息主题设计。
```
**位置**：第4章 4.1 网络通信模块

---

### AI 68% - 前端界面设计
```
### 4.4.2 前端界面设计

系统前端采用React + TypeScript技术栈，使用Tailwind CSS进行样式设计。
```
**位置**：第4章 4.4.2 前端界面设计

---

### AI 68% - 本地存储实现
```
（4）**本地存储实现**：利用SQLite完成了消息、联系人、群组等数据的本地持久化，支持历史查询和全
文搜索，保障用户对自身数据的完全掌控。
```
**位置**：第6章 6.1 工作总结

---

### AI 67% - NaCl Box加密方案
```
NaCl（Networking and Cryptography library）是一个开源的密码学库，由密码学专家Daniel J. Bernstein设
计。NaCl的设计理念是提供简单、安全、高性能的密码学原语，避免开发者因误用密码学API而导致安全问
题。
```
**位置**：第2章 2.3.1 NaCl Box加密方案

---

### AI 67% - NaCl Box安全特性
```
NaCl Box的设计具有以下安全特性：
（1）**前向安全性**：由于每次加密都生成临时密钥对，即使长期私钥泄露，也无法解密之前的历史消息。
（2）**认证加密**：XSalsa20-Poly1305同时提供保密性和完整性保护，攻击者无法篡改密文而不被检测。
```
**位置**：第2章 2.3.1 NaCl Box加密方案

---

### AI 67% - 客户端消息流程
```
客户端连接到本地LeafNode后，发送和接收消息的流程如下：发送消息时，客户端将序列化后的EncWire
消息发布到主题（如dchat.dm.<cid>.msg）；本地LeafNode接收到消息后，自动转发到远程Hub；Hub根据
主题将消息路由到订阅者；接收消息时，Hub将消息推送到接收方的LeafNode，LeafNode再推送到本地客户
端；客户端调用注册的回调函数处理消息。
```
**位置**：第4章 4.1.2 NATS客户端与本地LeafNode连接

---

### AI 67% - 本地存储模块
```
**本地存储模块**：负责SQLite数据库操作、消息CRUD、历史查询、搜索功能等。主要代码位于
internal/storage/目录。
```
**位置**：第3章 3.3.3 模块划分

---

### AI 67% - 自动保存
```
（1）**自动保存**：所有发送和接收的消息自动保存到本地SQLite数据库。
```
**位置**：第3章 3.1.4 本地历史记录

---

### AI 67% - 数据主权需求
```
数据主权是本系统的核心设计理念，需要满足以下需求：
（1）**数据本地化**：所有用户数据（聊天记录、联系人、群组信息）必须存储在用户本地，不得上传到
服务端。
```
**位置**：第3章 3.2.2 数据主权需求

---

### AI 67% - 私聊加密实现
```
### 4.2.2 私聊加密实现

如图13所示，私聊采用NaCl Box端到端加密，群聊采用AES-256-GCM对称加密，两种方案各有特点。
```
**位置**：第4章 4.2.2 私聊加密实现

---

### AI 67% - 加密流程
```
**加密流程**：

（1）生成临时密钥对：发送方生成临时的X25519密钥对（ephemeralPriv, ephemeralPub）。
（2）计算共享密钥：使用临时私钥和对方公钥通过ECDH计算共享密钥sharedKey。
```
**位置**：第4章 4.2.2 私聊加密实现

---

### AI 67% - 消息字段设计
```
设计原则：最小化暴露信息，cid、sender、nickname为明文（用于路由和展示），业务内容加密。需要澄
清的是，虽然cipher字段包含加密后的业务内容，但EncWire结构本身以明文JSON形式传输。nonce是
base64编码的随机字节，不是加密数据，用于接收方正确解密cipher。
```
**位置**：第4章 4.2.2 私聊加密实现

---

### AI 67% - 与云端方案的区别
```
**与云端方案的区别**：

本系统的数据存储设计与传统云端方案有本质区别。传统方案中，所有数据存储在云端服务器，服务端掌
握所有用户数据；本系统中，所有数据存储在用户本地，服务端只中转加密消息，不存储任何用户数据。
```
**位置**：第4章 4.3.1 数据库设计

---

### AI 65% - JetStream消息系统
```
JetStream是NATS内置的持久化消息流系统，为NATS提供消息持久化、流处理、消费者组等高级功能。
本系统的所有消息传输均基于JetStream实现，确保消息可靠传输和离线消息支持。
```
**位置**：第2章 2.2.1 JetStream消息系统

---

### AI 65% - Consumer消费者
```
（2）**Consumer（消费者）**：从Stream中消费消息的实体。本系统使用Pull Consumer模式，客户端
主动拉取消息，便于控制消费速率。
```
**位置**：第2章 2.2.1 JetStream消息系统

---

### AI 65% - 消息发布
```
（1）**消息发布**：应用通过JetStream API（`js.Publish`）发布消息，消息被持久化到对应的Stream中，
同时推送给当前在线的订阅者。
```
**位置**：第2章 2.2.1 JetStream消息系统

---

### AI 65% - 系统模块划分
```
根据功能需求，系统划分为以下模块：

**网络通信模块**：负责NATS连接管理、消息发布订阅、LeafNode管理、Hub集群连接等功能。主要代
码位于internal/leafnode/和internal/nats/目录。
```
**位置**：第3章 3.3.3 模块划分

---

### AI 65% - 加密安全模块
```
**加密安全模块**：负责密钥生成与管理、消息加密解密、数字签名与验证等功能。主要代码位于
internal/chat/nsc_crypto.go和internal/chat/crypto.go。
```
**位置**：第3章 3.3.3 模块划分

---

### AI 65% - 可扩展性需求
```
系统需要具备良好的可扩展性，满足以下需求：
```
**位置**：第3章 3.2.4 可扩展性需求

---

### AI 63% - 无单点故障
```
（3）**无单点故障**：集群内节点地位平等，没有主节点。任一节点故障只会影响连接到该节点的客户端，
不会影响整个集群的服务。
```
**位置**：第2章 2.2.2 NATS Routes与集群架构

---

### AI 63% - 集群边界与限制
```
**集群边界与限制**：当前实现采用固定集群配置。这源于技术取舍：若不支持离线消息（不用JetStream），
纯NATS的Routes集群支持动态新增节点，新节点可链式连接到一个节点后自动发现集群内其他节点；若支
持离线消息（使用JetStream），集群节点必须固定，新节点无法动态加入，这是由JetStream使用的Raft共识
算法限制的。
```
**位置**：第4章 4.1.4 Hub集群Routes配置

---

### AI 63% - 离线消息同步
```
### 4.3.3 离线消息同步

离线消息同步是系统的重要功能，保证用户不在线时也能接收消息。
```
**位置**：第4章 4.3.3 离线消息同步

---

### AI 63% - 可用性需求
```
系统需要具备良好的可用性，满足以下需求：
（1）**跨平台支持**：系统需要支持主流桌面操作系统，包括Windows，Linux和MacOS。
```
**位置**：第3章 3.2.3 可用性需求

---

### AI 62% - 解密流程
```
**解密流程**：

（1）接收方收到EncWire消息后，提取sender字段识别发送者。
（2）从好友列表中获取发送者的公钥。
（3）使用自己的私钥和发送方的公钥，结合消息中的Nonce，通过NaCl Box解密cipher字段。
（4）验证MAC确保消息未被篡改。
```
**位置**：第4章 4.2.2 私聊加密实现

---

### AI 62% - 对称加密
```
（3）**对称加密**：使用共享密钥和随机生成的Nonce，通过XSalsa20-Poly1305加密明文，生成密文和认
证标签。
```
**位置**：第4章 4.2.2 私聊加密实现

---

### AI 62% - 常量时间算法
```
（3）**常量时间算法**：所有加密操作都使用常量时间算法，避免时序侧信道攻击。
```
**位置**：第2章 2.3.1 NaCl Box加密方案

---

### AI 61% - 端到端加密
```
（3）**端到端加密**：所有消息采用端到端加密，只有通信双方能够解密。服务端即使被攻破，也只能获
取密文，无法获取明文内容。
```
**位置**：第2章 2.1.4 本项目架构设计

---

### AI 60% - SQLite搜索能力
```
虽然SQLite的LIKE搜索不如专业的全文搜索引擎强大，但对于桌面应用的规模已经足够。如果需要更强
大的搜索能力，未来可以考虑集成SQLite的FTS（Full-Text Search）扩展。
```
**位置**：第4章 4.3.2 消息搜索与历史查询

---

## 轻度疑似（50%-59%）

### AI 58% - 消息发送
```
**消息发送**：前端调用SendDirect()或SendGroup()方法发送消息。后端接收消息内容，查找接收方公钥
或群密钥，加密消息，构造EncWire，通过NATS客户端发布。
```
**位置**：第4章 4.4.3 核心功能实现

---

### AI 58% - 工作总结
```
本文围绕去中心化即时通信这一主题，设计并实现了一套基于NATS的消息系统。经过深入研究与工程实
践，主要完成了以下几方面工作：
```
**位置**：第6章 6.1 工作总结

---

### AI 56% - 联邦式架构
```
联邦式架构由多个独立服务器（节点）组成网络，用户可选择信任的节点注册，节点间通过标准协议互联。
工作流程：用户注册后获得节点域内唯一标识（如user@node.com）；同节点通信流程类似中心化；跨节
点通信需经双方节点转发。
```
**位置**：第2章 2.1.2 联邦式架构

---

### AI 56% - 消息暂存
```
（1）**消息暂存**：当接收方不在线时，消息被暂存在Hub的JetStream中，等待接收方上线后拉取。
```
**位置**：第3章 3.1.3 离线消息功能

---

### AI 55% - LeafNode启动流程
```
LeafNode的核心启动流程如下：首先解析配置中指定的Hub URLs，然后构建包含远程连接信息的Server
选项，接着创建并启动NATS Server实例，最后将Server实例保存到管理器中以便后续管理。
```
**位置**：第4章 4.1.1 应用内置的NATS LeafNode

---

### AI 54% - LeafNode启动流程（另一处）
```
LeafNode的核心启动流程如下：首先解析配置中指定的Hub URLs，然后构建包含远程连接信息的Server
选项，接着创建并启动NATS Server实例，最后将Server实例保存到管理器中以便后续管理。
```
**位置**：第4章 4.1.1 应用内置的NATS LeafNode

---

## 统计汇总

| 疑似度等级 | 段落数量 | 占比 |
|-----------|---------|------|
| 高度疑似（≥70%） | 45段 | 约28% |
| 中度疑似（60%-69%） | 35段 | 约22% |
| 轻度疑似（50%-59%） | 5段 | 约3% |
| 合计 | 85段 | 约53% |

### 高度疑似段落分布（按章节）

| 章节 | 数量 |
|-----|------|
| 摘要 | 2段 |
| 第1章 | 2段 |
| 第2章 | 8段 |
| 第3章 | 3段 |
| 第4章 | 22段 |
| 第6章 | 8段 |

### 建议重点修改的段落

1. **AI 94%** - NATS Routes集群机制（第2章）
2. **AI 93%** - 摘要第一段
3. **AI 91%** - 密钥派生原理（第4章）
4. **AI 86%** - Hub认证机制（第4章）
5. **AI 84%** - 跨平台应用开发总结（第6章）
6. **AI 83%** - Ed25519密钥紧凑特性（第2章）
