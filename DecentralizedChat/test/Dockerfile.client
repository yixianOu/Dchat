# P2P节点客户端 Dockerfile（使用本地编译好的二进制文件）
# 
# 使用方法:
# 1. 本地编译: make build
# 2. 运行: docker run -it --rm --name p2p-node p2p-node
# 3. 容器内执行: /p2p_node -node-id Alice -signal-server http://x.x.x.x:8080

FROM busybox:latest

WORKDIR /

# 复制本地编译好的二进制文件
COPY dist/p2p_node .

# 设置交互式环境
ENV TERM=xterm-256color

# 默认启动 sh，用户可以手动运行 p2p_node
CMD ["/bin/sh"]
