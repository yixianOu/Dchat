# 信令服务器 Dockerfile（使用本地编译好的二进制文件）
# 
# 使用方法:
# 1. 本地编译: make build
# 2. 运行: docker run -d -p 8080:8080 --name signal-server p2p-signal-server

FROM busybox:latest

WORKDIR /app

# 复制本地编译好的二进制文件
COPY dist/signal_server .

EXPOSE 8080

ENTRYPOINT ["./signal_server"]
CMD ["-port", "8080"]
